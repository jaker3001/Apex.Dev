# Apex Assistant - Dependencies
# Python 3.10+ required

# Claude Agent SDK (latest)
claude-agent-sdk>=0.1.17

# Environment variable management
python-dotenv>=1.0.0

# Async support (included with SDK but explicit for clarity)
anyio>=4.0.0

# API Server (for web UI)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
websockets>=12.0
pydantic>=2.5.0

# Authentication
PyJWT>=2.8.0

# File processing
pypdf>=4.0.0
pillow>=10.0.0

# Anthropic API (for title generation)
anthropic>=0.40.0

# Supabase (PostgreSQL backend)
supabase>=2.0.0
postgrest-py>=0.13.0

# Structured logging
structlog>=24.0.0
